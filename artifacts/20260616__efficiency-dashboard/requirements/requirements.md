# 效能看板需求文档

## 背景与目标

当前报表页主要展示使用覆盖和阶段分布，缺少“AI Native 流程是否带来实际效能增量”的表达。新增效能看板，用 DirectUS 真实 Harness 需求数据计算当前归档周期，并展示复杂度进度分布和阶段状态分布。

## 验收标准 (AC)

- [x] AC1: 侧边菜单保留统一“报表”入口，报表页内保留原使用看板和报表表格菜单。
- [x] AC2: 效能页展示 P85、P50、平均归档周期和需求数量。
- [x] AC3: 效能页不再展示旧流程访谈基线、提效百分比和对比图表。
- [x] AC4: 效能页展示复杂度进度分布，并可点击阶段状态色块弹框查看对应需求明细。
- [x] AC5: 当前报表页的两张使用分布图表折叠为独立模块，避免占据主要空间。
- [x] AC6: 真实数据统计口径已沉淀到 `background/tech/mock-and-todos.md`。
- [x] AC7: 前端构建与静态检查通过。
- [x] AC8: 统计时不再特殊排除开发者账号 `zhaoyue`。
- [x] AC9: 效能页展示 P85、P50 和平均周期。
- [x] AC10: 效能页按 PM、FE、BE、QA、归档各阶段 status 展示多维进度分布。
- [x] AC11: 效能页移除顶部统计说明模块，趋势图不再横向溢出父级。
- [x] AC12: 阶段进度分布使用红黄绿三色紧凑展示，并将总需求数统一放到模块标题右侧。
- [x] AC13: 页面整体不再左右溢出，右侧卡片在父级宽度内可见。
- [x] AC14: 阶段颜色语义调整为红色开始、黄色进行、绿色结束。
- [x] AC15: 各阶段需求明细展示全部参与评估的 Harness 需求，而不仅是归档完成需求。
- [x] AC16: `reports` 与 `efficiency` 合并为一个 `/reports` 菜单入口，效能看板作为报表页内部视图。
- [x] AC17: 总体页面根路径 `/` 默认进入 `/reports`。
- [x] AC18: `/reports` 默认展示效能看板，分段切换顺序为“效能看板 / 使用看板”，使用看板在右侧。

## 范围

### 包含

- 前端新增效能看板视图并合并到报表页。
- 扩展 DirectUS 报表数据读取。
- 当前报表页布局优化。
- 文档和真实数据统计口径同步。

### 不包含

- 不新增后端接口。
- 不接入真实状态变更历史。
- 不启动本地常驻服务。

## 依赖

- DirectUS `ops_issue` 数据。
- 用户确认 issue 表 `updated_at` 可作为最后状态变更时间。
- 用户确认 `harness_archive_status = 2` 才表示归档完成。
- 用户确认开发者账号 `zhaoyue` 参与评估统计。
- 用户要求复杂度卡片从左上到右下固定为 L、M、S、XS，并在 P50 右侧增加平均值；所有周期统一使用“天”作为单位。
- 用户要求不只看归档周期，还要根据 PM、FE、BE、QA、归档各阶段 status 多维展示进度。
- 用户要求将“归档周期趋势”改为“各阶段周期趋势”，将“归档需求明细”改为“各阶段需求明细”。
- 用户要求移除效能页顶部说明模块，整体宽度使用父级 100%，不要横向溢出。
- 用户要求阶段进度分布使用红黄绿三色，更紧凑展示，并将右侧需求数量统一放到标题区域。
- 用户根据截图反馈：右侧卡片看不到、左右仍溢出；红黄绿语义应表示开始、进行、结束，且颜色不要纯色。
- 用户反馈“各阶段需求明细”只有两条数据；原因是此前误用了归档完成数据源，需改为全量 Harness 需求明细。
- 用户要求基于最新代码，将 `reports` 和 `efficiency` 合并成一个 `reports`。
- 用户要求效能看板作为默认视图，使用看板放右侧，整体根目录默认进入 `reports`。
- 用户确认移除访谈基线、提效百分比和旧流程估算对比。
